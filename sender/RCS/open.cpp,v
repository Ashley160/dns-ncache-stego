head	1.2;
access;
symbols;
locks
	ashley:1.2; strict;
comment	@// @;


1.2
date	2023.09.12.03.36.37;	author ashley;	state Exp;
branches;
next	1.1;

1.1
date	2023.09.12.03.27.08;	author ashley;	state Exp;
branches;
next	;


desc
@Init RCS manage file
This is binary read file.txt but buffer cannot modify
@


1.2
log
@buffer suitable
@
text
@// read a file into memory
#include <iostream>     // std::cout
#include <fstream>      // std::ifstream
#include <bitset>
using namespace std;

int main () {

  std::ifstream is ("file.txt", std::ifstream::binary);
  if (is) {
    // get length of file:
    is.seekg (0, is.end);
    int length = is.tellg();
    is.seekg (0, is.beg);

    char * buffer = new char [length];

    std::cout << "Reading " << length << " characters... " << endl;
    // read data as a block:
    is.read (buffer,length);
		for(int i=0; i<length; i++)
			cout << bitset<8>(buffer[i]) << endl;

    if (is)
      std::cout << "all characters read successfully.";
    else
      std::cout << "error: only " << is.gcount() << " could be read";
    is.close();

    // ...buffer contains the entire file...

    delete[] buffer;
  }
  return 0;
}
@


1.1
log
@Initial revision
@
text
@d1 3
a3 2
#include <iostream>
#include <fstream>
d7 1
a7 2
int main() {
    char buffer[256] = {0};
d9 26
a34 11
    std::ifstream ifs("file.txt", std::ios::in | std::ios::binary);
    if (!ifs.is_open()) {
        cout << "Failed to open file.\n";
        return 1; // EXIT_FAILURE
    }

    ifs.read(buffer, sizeof(buffer));
		for(int i=0; i<sizeof(buffer); i++)
    	cout << "i=" << i << "\t" <<  bitset<8>(buffer[i]) << endl;
    ifs.close();
    return 0;
@
